# OperatingCurta sixth-station 3-ULP bank difference

This is a diagnostic record, not a change to evaluator, motion or float comparison behavior. The ordinary viewer under test was commit `6da0003d6154676bc807ac9eb8e0963a12febecb`, installed bundle SHA256 `8a03c3a3d3cb83c132059474092a15e7b2a085871d98d87cdcccbd40566bfe81`. The isolated sixth-station OperatingCurta export had manifest SHA256 `f58da4f21142d452bd6de58adc901fafa131d81fcaaf301df7fc767f2269dc67`, program identity `4127d33c87f1501a024ee5c7391d1897ce3af65309b946c811d4244c7fd68da2`, and 213 coordinates. Its project-owned reports are `_build_counter_bank_sixth_c895dba/counter-browser-acceptance.json` and `_build_checks/counter-bank-sixth-python-2026-09-22.json` under `projects/Calculators/Curta-Type-I-3x/`; no project source was changed for this viewer diagnosis.

The sixth-station short and long requests stopped, replayed, relieved and retried correctly. In each stopped and idle full bank, 212 coordinates were exactly equal to the Python oracle. The remaining coordinate, `carriage.registers.dial_detents.p_6mm_ball_419241_9.lift`, was browser `0.08929057589867746` (`0x1.6dbbf4753694dp-4`) versus Python `0.0892905758986775` (`0x1.6dbbf47536950p-4`), three binary64 ULPs apart. The strict full-bank hash comparison therefore failed; it has not been relabelled a pass. Discrete status, stop/contact and replay comparisons did not differ.

The published p9 law is edge 175, reading `carriage.registers.turns_register.p_10205_3.turn`; the source's final bank value is `-181.4500831` on both sides. Re-evaluating the published law in the unchanged JS engine against the final committed browser bank returns `0.08929057589867746`, exactly its stored value. The project's Python `dial_cam.rise(36)` at the same final source phase also returns that value. Direct Python and Node trig/sqrt intermediate binary64 bits matched for that scalar angle, so this is not evidence of a final-point libm discrepancy.

The source path during zero-duration preparation (`reverser_height→-3`, `crank_rotation→260`, `dt=.1`) reveals the actual origin. A temporary trace at `run/trajectory.ts`'s existing `lawMotion` non-retained piece loop recorded the JS p9 final modulo branch at `low=0.9444444444444443`, `high=1`: `base=0.05`, `from=0.05`, `evaluate(high)=0.08929057589867746`, hence `from + (evaluate(high)-base)=0.08929057589867746`. The paired Python path trace found a residual `from=0.050000000000000044` (`0x1.99999999999a0p-5`) at that branch; with the same `.05` base and `.08929057589867746` endpoint, its unchanged accumulation formula returns `0.0892905758986775`. The residual came from the preceding modulo-cut piece. Thus the difference is in piece-boundary accumulation, not in an altered law, dropped sample or changed stop. All temporary instrumentation was removed.

ADR-047 (`workflow/adrs/EXPORT/ADR-047-the-corpus-makes-the-two-runtimes-one-algorithm.md`) requires exact discrete agreement and float agreement within the producer corpus's stated relative tolerance; the committed running corpus currently states `1e-9`. A strict bitwise all-coordinate project gate is stronger. The pilot chose to preserve the original failing reports and separately record a narrowly identified, at-most-3-ULP exception for this p9 float while keeping every other coordinate, status, contact and replay exact; no blanket tolerance, rounding adjustment or engine workaround was made here.
