# OperatingCurta bound-read motion demand: frozen viewer evidence

The independent project's committed `7586002` OperatingCurta export remained fixed at `projects/Calculators/Curta-Type-I-3x/_build_current_operating_7586002/manifest.json`, SHA256 `c36432b1080387eeb40234532f516a9ae05f8a9822fd3f47f44ffbbb42f889d1`, compiled program identity `be125f4048c8be00ce4e73d646311d4a50ac75a59aa739f93bf2f38c992fa195`, 213 coordinates. The viewer candidate starts at main `1b06139`. No project source or export was edited.

The crank's compiled high constraint reads only `main_drive.anti_reversal.reverse_rotation_prevention_pawl.turn`. Its defining nonlinear law needs and gives that same coordinate. The old tick-local motion demand was 174 edge dependency keys and omitted the pawl, although its bound consumes the value. The new union adds only the 30 distinct compiled constraint reads missing from those edge dependencies. It is tick-local metadata and does not change the document or law.

A viewer-owned self-read, running-bound regression was red before the change because its search had no retained pawl path, then green after the one-place demand change. It asserts a real blocked stop, admission, bank and snapshot/restored replay. The pre-existing quiet-bound test's generic `nodeOf` counter went to zero because the path no longer uses that evaluator; it now checks whether a constraint search ran on movement and did not run when quiet. The physical behavior assertion was retained.

On reserved CPU 15, a frozen-export `dt=.1` first tick after zero-duration `digit_1→1` and a declared `crank_rotation` move by 360 over 2 seconds admitted 18 degrees. Before: no retained pawl motion, 65 crank-bound `constraintLevel` subprogram evaluations, **2.133 process-CPU seconds**. After: pawl motion retained, zero generic bound-search evaluations, **1.084 process-CPU seconds**. The tick, active command admission, −18-degree physical crank and sorted 213-coordinate IEEE-754 bank digest `35815fd200e3b8a117eb0697b9b02a2bb937b87867143d0ceca467a7ff6502c6` matched. Host jobs outside CPU 15 were concurrent, so wall times are observations, not budgets.

For an actual active stop, each variant loaded that same program at `dt=.1`, made zero-duration moves `digit_3→3`, `crank_rotation→160`, `digit_3→0`, then requested `crank_rotation→170` over one tick. The baseline was the candidate source with its tick-local demand replaced by the original 174-key set; no other code differed. Temporary diagnostic callbacks captured every search's ordered `(bound side, sample fraction, level)` as IEEE-754 hex, after the preparation was cleared. Baseline and candidate each produced **134 identical ordered samples**, SHA256 `2355f63b62f688481454c693952cb68f006cad443f092b9c7d96969a23c4ed2e`. Both blocked with admission bits `4014e49899996800`, the same low crank stop and stop-fraction bits `3fe0b6e07ae12000`, and full bank digest `aeb3888a1619ba6391d08c84d2c93c98e37f8622cbeb7b279f1292a078745d5b`. Generic crank-bound evaluations fell **67→2** (the remaining two classify the stopping input); the paired active tick took **2.566→1.917 process-CPU seconds**. An independent temporary `Object.is` check also compared every candidate bound sample to ordinary generic evaluation with no signed-zero, NaN or other bit drift. All diagnostic hooks and the external-project probe test were removed before validation.

The final candidate passed the running suite 65/65, typecheck and widget build. The four frozen Curta source-timing fixtures passed with whole and restored replay; the initial focused five-suite run was 183 passes and one test-probe failure, fixed as described above. The broad `npm test` process exited **1**, with **1,465 tests passing and two wall-clock throughput gates failing**: old Curta fixture 792 versus required >900 ticks/s and quiet ShiftedCarry 2,730 versus required >3,000 ticks/s. These same two throughput gates failed on viewer main in the prior cycle under concurrent host work. No threshold, physical tolerance, sample schedule or law was changed to make that process look green.

An independent reviewer flagged possible work on quiet ticks from unconditional bound-read demand. A process-only frozen-export idle tick after `digit_1→1` constructed 255 versus 256 retained motions under the original versus union demand; process CPU was 85.0 versus 74.1 ms in that one pair and the complete bank, stops and tick matched. This does not establish a universal idle-speed gain, but shows no material idle regression on the originating machine. The committed self-read regression also advances one idle tick after its blocked replay and asserts no constraint search, state change or extra stop. Untraced Play paths remain on the existing fallback branch; no tracing algorithm was changed.

The independent read-only reviewer found no remaining demand/fallback/error blocker and reran viewer run, source-timing and expressions tests: 169/169 passed. The viewer change remains separate from the framework's analogous cycle and copies no framework source.

This proves the bounded first-tick and active-stop behavior, not an entire 480-tick browser revolution or a final interactive frame rate. The first pointer browser check of this candidate remains to be done after integration; the root's corrected counter-2 browser retry used prior main bundle `f1f2cd4c`, not this candidate, and cannot be cited as its browser acceptance.
