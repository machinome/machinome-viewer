# Exact absolute-target endpoint: implementation evidence

The originating Curta asymmetric v13 export is pinned at manifest SHA-256
`3388020498a62ea4d75b4adfa7acd1b995e098d47bc101dfd9b8e6c0cc6c3587`,
program identity `200219dcb6356bb3d27324e22f8dce01fdc5779fdb0ba2b06a9c56c28be3c8e8`.
On the public one-input carriage with inclusive high `3.9075`, baseline
viewer main `b976a17` reports an erroneous block when an absolute request
starts at the adjacent float above `-4.9425` and asks for `to=3.9075`:
the old `start + (target - start)` reconstructs a value beyond high.

The isolated viewer cycle retains the authored native target separately from
the legacy travel. It uses that target only at a full terminal admission,
propagates eligible exact end landings before Bound judgment, and replays a
genuine partial stop without a target override. The actual Curta nine-case
hosted report on intermediate bundle `f0862fd5...` passed all nine statuses
and all 214 coordinate bits against the paired producer candidate, including
real pointer admissions and an inspected screenshot. Its immutable report is
`projects/Calculators/Curta-Type-I-3x/_build_checks/reverser-asymmetric-browser-ffafa3f-05.json`
(SHA-256 `a01dd2374990b641b24fafb239269f1c410ef4397b621190b8d88d61e1d47afa`).
That bundle predates a separate synthetic planned-law landing fix and is **not**
the final acceptance bundle.

After the stateful-call fallback, a separate no-WebGL Run check on the same
frozen manifest and report05 Python oracle matched all nine statuses and all
9 × 214 coordinate IEEE bits (zero differing keys). Its diagnostic source is
`/tmp/endpoint_curta_no_webgl.mjs` SHA-256
`99ffba5e41caccf12db9ee8b3bd6b628c3279575d9bba685094bbbfe623b2217`.
The viewer source diff was `28659e20bbf082af7e74c983ff9548e6e26a23ed16310ee80a83526d28a9db9d`
and the built candidate bundle was SHA-256
`41bd9f1ea95162e8cbbaafef8a247f84cada95ff63082533dce2cdc91e77d58e`.
The final hosted report06 is terminal green against frozen producer source
`run.py` SHA-256 `95a7f23420fa9e67c1b94d1e3446bead1f07380e0ac94b2cc322f1f239d5fa01`
and `trajectory.py` SHA-256
`96483106789792d99a9413beb9e585b2eab5e64063778ec583d327d46d2bd679`,
with the final viewer bundle `41bd9f1e…`. It matched all nine expected
statuses and all 9 × 214 bank IEEE bits. The real pointer gesture admitted
`-1`, `-1`, then `-0.8480000000000061`, stopped at lever
`1.0594999999999941`, observed pointer release, left no active commands,
and changed no unrelated drivers or turns. The browser had no page errors;
the screenshot was inspected at crank 90 and time 0.67. Preserved artifacts:
`projects/Calculators/Curta-Type-I-3x/_build_checks/reverser-asymmetric-browser-85c8710-06.json`
SHA-256 `3a77aeec6f3779b11fa6aaf23602aaecdd23e3a3782ce82ab2b15e311d0cc433`,
PNG SHA-256 `71aed3d5e7182aa9d824ec5f74483a82d2765f4d47e3b30bf342db730a18d97b`,
log SHA-256 `ed24b500ddfc2c656772e8eec8f2f7f73f89091d76e4ee34936d7a8e620a102f`.
This validates the viewer capability on the current Curta export; it does
not assert complete mechanical fit or production adoption of other trial work.

Focused red/green controls cover the exact first and second `to` endpoints,
adjacent start, real outward stop, mixed same-tick blocked and completed
commands, multistep snapshot/replay, signed zero, retained Play and Follow,
affine interior probes, dynamic Bound samples, selected/inactive cyclic-block
reads, and a planned curved-domain/history witness. The viewer run file now
passes 110 focused tests. After the last integer-snapshot guard was added,
the final full widget suite passed 1,556 tests with two skipped and one
skipped file; `tsc --noEmit`, strict OpenSpec validation and `git diff --check`
all passed. The bundle bytes are unchanged by that test-only addition.

An independent JS-only adversary found that extra terminal re-evaluation of
`random(1)` changed a law's call count from two to three; forcing it untraced
amplified a demanded Bound case from two to 130 calls. The corrected branch
retains the original traced motion and two-call cadence, while the input itself
lands at its exact target. A binding alias to `random(1)` is covered. The
evaluator's `sin` wrapper delegates to live `Math.sin`; a pre-import host
replacement was likewise observed to alter call counts, so live-delegating
wrappers take the legacy descendant path. A pre-import `Proxy(Math.sqrt)` can
masquerade as a native function and remains outside the explicitly stated
trusted-standard-Math platform precondition. Native-string inspection is a
best-effort rejection of obvious replacements, **not** a tamper-proof
certificate. Standard `sqrt` retains the valid curved endpoint behavior.

The paired framework producer's independent full-suite gate passed 3,680
tests with four skipped and 2,230 subtests in 402.94 seconds. No viewer
archive, integration, push or publication is claimed here.
