## 1. Documentation foundation

- [ ] 1.1 Add red-first checks for manual presence, complete public API coverage,
  documentation metadata, and workflow/user-documentation separation.
- [ ] 1.2 Relocate development records without changing accepted decisions and
  build the pinned Sphinx/Read the Docs configuration and metadata.

## 2. User manual

- [ ] 2.1 Write installation, operation, sharing/capture, compatibility and
  troubleshooting guides with honest publication and dependency boundaries.
- [ ] 2.2 Document all public mounts, options, handle members and CLI operations
  with units, results, lifecycle and refusal semantics.
- [ ] 2.3 Provide an executable interactive embedding example from a committed
  fixture and the built viewer bundle, without framework dependencies.

## 3. Validation and review

- [ ] 3.1 Pass strict Sphinx, documentation coverage, widget and relevant Python
  checks; verify source distribution content and document any limitations.
- [ ] 3.2 Inspect desktop and mobile pages, search, links, and the real embedded
  viewer; record evidence and run the site for pilot review.
- [ ] 3.3 Obtain pilot approval, then sync the baseline spec, archive the change
  and commit the completed implementation record. Do not integrate or publish.
