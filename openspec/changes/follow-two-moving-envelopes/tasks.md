## 1. Producer contract and independent review gates

- [ ] 1.1 Obtain the producer's accepted version-12 Follow wire, generated corpus and numeric reference with actual Curta boundary-path/stop/relief/replay proof; reconcile this proposal's provisional fields and algorithms before any viewer runtime edit.
- [ ] 1.2 Resolve independent review of one-sided Curta chart values, sub-1/64 transient inversion, representable cut-side contact versus atomic unsupported-precision refusal, opposing-source attribution and required matching Bounds.
- [ ] 1.3 Commit the corrected viewer planning artifacts only after the producer contract and peer review agree; do not present this initial sketch as pilot-ratified design.

## 2. Red-first consumer contract

- [ ] 2.1 Add failing loader/capability tests for valid version-12 Follow and malformed source/follower, writer, graph, boundary plans, Bounds, terminal-only output and rest interval; pin version-1–11 behavior.
- [ ] 2.2 Add failing motion tests for outward push/retreat retention, inward push, interior excursion, both sides of a numeric cut, curved/refused paths, Python-order ±0 tie bits and exact absolute landing.
- [ ] 2.3 Add failing admission tests for opposing source pushes, sub-grid transient inversion, unrepresentable micro-contact refusal, stopped/replayed/relieved requests and complete bank parity.

## 3. Viewer execution

- [ ] 3.1 Load and validate the accepted Follow edge and producer-authored boundary plans only under version 12; enforce matched Bounds and terminal-only output, and construct certified finite piecewise-affine boundary paths with distinct one-sided cut values.
- [ ] 3.2 Implement producer-order retained projection over the full path, carrying exact absolute landing and refusing uncertified curved/instantaneous-contact cases without partial commit.
- [ ] 3.3 Integrate Follow with dynamic Bound prefix replay and certified conflict candidate cuts; preserve existing bisection, attribution, snapshot/replay and old law/Play paths.
- [ ] 3.4 Update the single viewer API/document-version declarations only after executable parity is green; rebuild the bundle and verify entry point, command and browser-global reports agree.

## 4. Paired acceptance and integration

- [ ] 4.1 Copy the accepted producer corpus and pass complete bank, outcome, stop and replay parity, including IEEE nextDown/cut-side fixtures; rerun all legacy corpus tests and typecheck.
- [ ] 4.2 Pin a fresh production Curta version-12 export, prove no-WebGL full-bank stop/relief/replay against Python, and exercise actual hosted and standalone browser controls with inspected pixels and terminal state.
- [ ] 4.3 Run broad widget, viewer Python, documentation and distribution checks; record any environmental throughput failures without weakening floors, and amend the viewer ADR/changelog under the manual discipline.
- [ ] 4.4 Sync specs, archive the viewer-owned change and integrate the isolated worktree into current viewer main only after paired final-content validation; rebuild the installed primary bundle, with no push or publish.
