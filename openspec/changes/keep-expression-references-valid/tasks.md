## 1. Approval and failing evidence

- [x] 1.1 Obtain pilot ratification of this proposal, design and delta; record it and commit the planning artifacts before implementing.
- [x] 1.2 Inventory retained node references and reset entry points across bindings, program classification, kink levels, path values, clocked execution and posing; confirm the scoped design covers every holder or return a revised design for approval.
- [x] 1.3 Add deterministic red-first tests for reset during multi-entry binding preparation and root-before-bindings classification, including an old ID reused by a different node and a working set larger than the threshold.
- [x] 1.4 Add red-first retained-reference regressions for derived kink operands and subsequent evaluation after reset; retain existing malformed-table refusal coverage.

## 2. Coherent expression lifetime

- [x] 2.1 Implement the internal nestable synchronous preparation/use scope and exception-safe deferred reclamation, without raising or disabling the existing threshold.
- [x] 2.2 Make binding maps and root acquisition coherent within that scope; update all audited consumers and generation-qualified derived references without altering numerical algorithms.
- [x] 2.3 Verify nested scopes, exception cleanup, multi-document binding isolation, repeated replacement, retained-node bounds and final disposal; record counts against bounded working sets rather than publication history.

## 3. Viewer regression and originating project acceptance

- [x] 3.1 Run expression, binding, running and clocked suites plus existing numeric conformance without changing expected values; run the full widget suite and typecheck.
- [x] 3.2 Build the uninstrumented bundle and run viewer Python/browser tests, recording skips and environmental failures separately from passes.
- [x] 3.3 Re-run the pinned Curta export through its public mount reproducer; require successful mount, initial/idle state and no cache-related page or worker errors. Record bundle/document identities, mount and first-step times, and peak node counts without claiming a Python speedup.
- [x] 3.4 Complete the existing hundreds/eighth-station blocked-motion, relief/retry and replay checks; compare captured 213-coordinate banks with project-owned Python evidence, capture and inspect the mounted model, and report any independent failure without broadening this repair.

## 4. Durable completion

- [x] 4.1 Record red/green evidence and scoped lifetime amendments to ADR-043/044, preserving page-scoped sharing and document-local bindings.
- [ ] 4.2 After all acceptance gates pass, synchronize the ratified delta, validate and archive this viewer change, and commit the implementation record in the viewer repository only. Do not push, publish or infer integration authority.

Implementation and acceptance are complete; see `implementation.md`.
Task 4.2 stays open pending the pilot's response to the archive workflow's
explicit sync/archive confirmation. The code/evidence checkpoint is committed
without moving this change or altering the baseline specification. No push,
publication or cross-repository integration is performed.
