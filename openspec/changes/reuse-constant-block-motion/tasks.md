## 1. Characterize and reconcile

- [ ] 1.1 Confirm the concurrent terminal-target cycle's `Motion.exactTerminal` contract, merge its integrated viewer history into this isolated worktree, and rerun planning validation before code changes.
- [ ] 1.2 Add red-first focused fixtures for a repeated pure constant block, changed input bits, `+0`/`-0`, terminal metadata, interior leave-return motion and different cuts; assert path `at(0)`, interior `at(t)`, `at(1)`, `cuts()` and `exactTerminal`, plus complete bank, crossings and stops against uncached execution.
- [ ] 1.3 Add red-first fixtures for stateful/shadowed/custom expressions, malformed operands and first-error order, crossing/landing output, refused integration/retry, restore/reset/new Run, and the one-entry-per-block bound.

## 2. Implement narrow private reuse

- [ ] 2.1 Implement conservative deterministic numeric graph provenance and exact finite input/path keying for compiled blocks; make every preflight uncertainty fall back without evaluating expressions early.
- [ ] 2.2 Thread one Run-owned candidate cache through propagation, publish immutable scalar descriptors only after a successful effect-free integration, and invalidate on successful restore/reset.
- [ ] 2.3 Reconstruct fresh constant output Motions and increments with exact endpoint/terminal bits on hits; retain the ordinary block walk for all other cases and keep crossing-limit and Bound behavior unchanged.

## 3. Verify on the originating machine and repository

- [ ] 3.1 Run focused tests, full widget tests, typecheck and Python viewer tests; preserve document API 26/version 13 and the old-version corpus.
- [ ] 3.2 Run paired CPU-12 no-WebGL baseline/candidate on the pinned Curta normal 18°, zero-duration 90° prep, three 48-tick lever gestures, blocked/retry/restore and exact 214-bank/ordered record oracle; report process CPU separately from browser time and reject a nonmaterial or divergent result.
- [ ] 3.3 Validate the final merged terminal-target plus cache history on a fresh export and hosted control path without changing timestep, search samples or numeric tolerance.
- [ ] 3.4 Reconcile any concurrent nonconflicting main advance in this isolated worktree, rerun the relevant gates, then archive the OpenSpec change and submit the focused viewer commits for integration without push or publication.
