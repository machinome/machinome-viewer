## 1. Reproduce and correct

- [x] 1.1 Commit producer-derived overtaking and following-contact fixtures and run them red in the unmodified browser executor. (Included in the single completion commit.)
- [x] 1.2 Implement local orientation and conservative exact constant-contact proof, preserving stationary/clocked paths and genuine refusals.
- [x] 1.3 Add negative proof, replay and transactional tests, and pass the complete widget corpus and typecheck.

## 2. Validate and integrate

- [x] 2.1 Build the bundle, run viewer Python/distribution checks and validate the actual Curta worker carry, restraint and replay requests; inspect pixels and record both content states.
- [x] 2.2 Record ADR disposition and acceptance evidence, synchronize specs, archive and validate the change, and commit the complete implementation record.

## Post-archive integration runbook (2.3)

Recheck clean unchanged main, fast-forward locally under existing pilot
authority, verify both commits and remove only this clean worktree without
deleting the branch. This necessarily follows the completion commit; the
originating project records the actual integrated pair afterwards.
