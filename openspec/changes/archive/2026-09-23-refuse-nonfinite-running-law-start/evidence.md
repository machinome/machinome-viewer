# Public-API baseline, 23 September 2026

- Viewer source: clean `main` commit `b976a172a7964b269183eaee289c8cf26668cd58`.
- Independent source finding: `WTs/running-target-endpoint/workflow/evidence/nonfinite-law-start-2026-09-23.md`; that evidence was uncommitted when this separate cycle opened.
- Reproduction: load its exact version-5 document through `loadProgram`, create `new Run(program, 1, 8)`, call `move('feed', { to: 0.1 })` from authored `feed=0.2, slide=0`, then read `command.status`, `tick()`, and `state()`.
- In this isolated worktree, a temporary Vitest diagnostic using only those public entry points passed on the baseline with `completed`, tick `0`, `feed=0.1`, and `Number.isNaN(slide) === true`. Command: `npx vitest run src/run/nonfinite-baseline-diagnostic.test.ts`; Vitest 3.2.7, one test passed. The temporary diagnostic was removed after the observation. No runtime source was edited.
- A second temporary public-API diagnostic used a producer-compatible rest bank, `feed=0, slide=sqrt(0.1)`. Immediate `move('feed', { to: 0.2 })` returned `completed` at tick 0, banked `slide: NaN` and recorded that poisoned bank. A two-tick move to the same endpoint committed tick 1 at `{feed: 0.1, slide: 0}` and then completed tick 2 with `slide: NaN`; the tick-1 bank and record are prior successful state that a refusal must retain. Vitest 3.2.7 passed the diagnostic; it was removed after recording the result.
- Running authority is `openspec/specs/viewer-package/spec.md`, “The step reproduces the producer's own integration” (~1740–1900): continuous law endpoint-minus-start arithmetic, atomic failure for a law that cannot be integrated, and retirement of commands moved in that failing step. The clocked executor requirement at ~3451 is separate and does **not** establish `Run` behavior. ADR-045 preserves in-thread/worker engine identity; ADR-047 pins only the producer corpus's recorded scenarios, not this unrecorded domain-error path.
- Producer comparison on clean framework main `a9f10b89`: a minimal `AssemblyNode` with `feed=Driver(default=0)` driving a revolute coordinate by `machinome.math.sqrt(0.1-feed)` constructs a `Sim` at `{feed: 0, shaft.turn: sqrt(0.1)}`. Immediate `move('feed', to=0.2)` raises raw `ValueError: math domain error`; bank, tick 0, and trajectory stand, but its failed `feed` command remains `active`. With `default=0.2`, the producer fails its rest render with the same domain error and cannot export the invalid-start document. Thus this viewer cycle cannot claim complete producer parity for command retirement until a separate framework correction is reviewed.
- Causal seam: `src/run/edges.ts` subtracts the law's start evaluation from its end evaluation; in this case finite end minus `NaN` start gives `NaN`. `Run.pass` in `src/run/run.ts` stores that increment without a finiteness guard, and `integrateScoped` commits the resulting `NaN` bank value and completes the command. Existing `UnsupportedLaw` already maps to the `law` refusal kind and is caught transactionally by `integrateScoped`.

This evidence does not establish that every non-finite evaluation shape is currently mishandled; source, destination, and path-interior cases must be covered red-first where the proposed guard applies. The companion producer command-retirement issue is not folded into this viewer change.

## Endpoint integration and producer-document gate

- The independent endpoint change reached viewer main `80d58d499e3f411fe972bb793ce2d29459e3d1a4`; this cycle merged that history before its completion. Its three new terminal-law evaluation sites (untraced end/start, affine terminal end/start, and traced authored end/start) receive the same law-result check, in their existing order.
- `tests/fixtures/nonfinite-running-law-v11.json` is the full document captured from `document(bound(SquareRootMachine()))` using the producer runtime at clean main `34b3de127165ec7c2ff9429e24fc01ab39416e5f` and this cycle's candidate `tests/test_running_nonfinite_law.py` machine declaration; that test class was not part of producer main at `34b3de1`. Its captured root `mtime` is retained. File SHA-256: `d8e278270a75d59247e747d5bde1a91d4b90194623077a067853538636852778`. `loadProgram` accepts that document; the same public `Run` refuses `feed: 0 → 0.2` and completes the finite `0 → 0.1` boundary.
- A paired exact-terminal probe uses `start=-0.234`, `target=0.10000000000000002`, and `start + (target - start) === 0.1`. The rounded additive endpoint is inside `sqrt(0.1-feed)`'s domain, whereas the actual authored target is outside it. Both producer `Sim` and viewer `Run` refuse the request without changing the bank or leaving an active command. This exercises the endpoint cycle's newly evaluated target rather than only the legacy law path.
- On the merged endpoint main plus this isolated, uncommitted law implementation, focused Vitest passed 10 law-refusal tests; the final full widget run passed 1,566 tests (2 skipped; 58 files passed, 1 skipped), including the existing producer corpus replay. `npm run typecheck` and `npm run build` passed; the resulting bundle is local only and nothing was pushed or published.
- Final source review removed a proposed post-propagation increment check: a finite law value could have an overflowing subtraction, and rejecting that arithmetic without matching producer evidence would exceed the ratified evaluated-law-result scope. The final full-suite, typecheck, and build counts above are from the reduced evaluation-only diff.
- Final source pins for those gates: viewer endpoint main `80d58d499e3f411fe972bb793ce2d29459e3d1a4` merged into this cycle's planning head `2e48b75f1409d1c0ee8a1dd56b06b46d285fb526`, plus the uncommitted law diff shown by this change. Local `npm run build` emitted `widget/dist/machinome-viewer.js` with SHA-256 `5b7f8903194a99d9b7cc4acbdaaf1997682e6218d418b036580a0ecf31da99e6`. The built artifact is not staged, published, or pushed.
