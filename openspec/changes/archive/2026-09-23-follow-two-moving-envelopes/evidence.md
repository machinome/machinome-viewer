# Consumer evidence

This viewer cycle consumes the framework producer at `f4c48f61a88f183f7e11ccb3e0bbc7158b8e6b9d`. Its frozen v12 wire fixture and ordered command corpus are copied byte-for-byte into `widget/src/run/follow-wrapped-v12.json` and `follow-wrapped-commands-v12.json`; the wire fixture SHA-256 is `21d80c45e535f71d6476185a48da24985dfe4c39b5d220510590ec879a6094c5`. The project finding is the OperatingCurta's free positioning ball between the bell and collar; the pre-Follow switch law pulled it backward on retreat and stopped a raised-carriage crank at zero. The producer's independent broad gate passed 3588 tests and 2124 subtests.

## Red-first and synthetic consumer checks

Loader tests first refused the producer's valid version-12 fixture; source and stop tests first exposed the missing retained projection and missed a long modulo excursion whose old 64 uniform samples all read home. The completed consumer covers malformed Follow wiring, plans, matching Bounds, terminal-only output and infeasible rest; outward/inward pushes and retreat; exact and left cut sides; signed-zero ordered ties; sub-grid and 128-period contact; opposing-source stop, relief, snapshot/replay; and atomic refusal of curved or unrepresentably narrow contact. The producer's complete command corpus matches status and every bank value. Independent read-only peer review found no source-level blocker and ran 178 focused program/run tests.

## Actual Curta parity, read-only project

The frozen project source was `projects/Calculators/Curta-Type-I-3x` at `4cd6ec4`; the FollowTrial definition is in the producer's `curta_follow_probe.py`. Neither this viewer cycle nor its diagnostic changed project source. A fresh no-mesh `document(FollowTrial())` produced an ignored 3,296,295-byte v12 document with 214 bank coordinates and 274 edges:

- `_build/follow_viewer/curta-follow-v12.json`, SHA-256 `aff53651e4b71c85ed5288a8e95997322f75c4a070ccd75205523491d5a33628`.
- `_build/follow_viewer/curta-follow-oracle.json`, SHA-256 `d1a428e1536444429c7bf8bf976edda1c9ffc025e43b60adb294bff1ecf558ac`, stores eight complete 214-coordinate Python bank stages.

A temporary Vitest diagnostic loaded those two ignored artifacts and compared every coordinate with `Object.is` at rest, lift, blocked crank, restored replay, relief, after-relief turn, outward turn and return. Both diagnostic tests passed in 36.74 seconds. Python and viewer exactly matched the raised-carriage ball at `-1.2252018554755857`, the first blocked crank angle `.36119713971311285`, the stopped snapshot on replay, and the retained outward ball through the return. The first diagnostic invocation hit Vitest's default 5-second timeout, then completed with explicit 180/300-second case limits; the failed invocation is not represented as a green gate. This is a no-mesh run, not visual or pointer acceptance of a final project export.

## Package and manual gates

- Final widget suite after the capability/manual assertions: 55 files / 1490 tests passed in 120.78 seconds on CPU 15; typecheck passed. Focused Follow/program/run corpus: 213 tests passed.
- Worktree bundle build succeeded (843.8 KB). The worktree-local `machinome_viewer describe` reported API 25 and document versions 1–12. No package was pushed, published or installed into the primary viewer.
- Viewer documentation tests: 7 passed. Strict Sphinx HTML build passed. Browser manual check passed 14 pages at 1440- and 390-pixel viewports, 204 local links, menu/search/live interactive/reference examples, with no browser errors; `_build/docs-review/report.json` and the inspected narrow running-reference screenshot hold the generated evidence.
- After the final Follow paragraph was added to the running manual, its 7 documentation tests, strict Sphinx rebuild and the same browser-manual matrix were rerun and passed (14 pages, 204 links, no browser errors).
- A first broad Python run was interrupted after 170.34 seconds when expected API-24 and 1–11 assertions failed against the newly built API-25 bundle (11 failed, 79 passed at interruption). Those source-capability expectations were updated, preserving historical/released fixtures. Focused bundle/CLI/manual rerun: 31 passed. The completed broad Python rerun passed **196 tests and 22 subtests** in 641.27 seconds, with 51 warnings, on CPU 14. The warnings are pre-existing browser/Pillow/dependency deprecations, not test failures.
- The worktree's temporary `node_modules` symlink into primary viewer was removed without touching its target; independent dependencies installed from the lockfile offline. `scripts/check-dist` then exited 0 from this worktree: sdist and wheel built, a throwaway environment installed the wheel and reported API 25, document versions 1–12, package 0.7.0 and an included bundle. Nothing was uploaded. The isolated package build's npm audit printed 3 moderate dependency advisories; the dry-run gate itself passed, and this cycle does not claim an audit/security fix.

The framework's v12 capability is integrated at the producer commit above. Final mesh/pointer acceptance requires the project owner's eventual production Follow adoption and new export; no claim about it is made here.

## Mesh-enabled originating-project trial

The producer's committed `FollowTrial` was exported through the public framework `export_node` path from the project directory, with `bind_declared_defaults` and a viewer-worktree-local `SOLID_BUILD_DIR`. This is explicitly a trial of the root-owned project source at `4cd6ec4`, not a production project adoption. The first unbound-driver call failed before artifact creation; the first bound call from the viewer worktree built STLs but failed the export containment guard because a project source path resolved relative to that cwd. The successful project-cwd call wrote only ignored viewer-worktree artifacts and produced a 40 MB standalone export with meshes, version 12, 214 coordinates:

- `_build/follow_mesh_trial/manifest.json`, SHA-256 `38c32cda3d2e5e31323b71f2dc232b17ddd189fc8e89f86cf75d36659d9c200d`.
- `_build/follow_mesh_trial/machinome-viewer.js`, SHA-256 `732087f7dd8ef22d51b3c2cb45f30b085966eab5d5e090485626cb0d50fe8530`, copied from this cycle's candidate bundle.
- `_build/follow_mesh_trial/index.html`, SHA-256 `16238dda75b0b88224bd1bcaf0c9dfd77a6f43d75652b2972f4af5ed00111bb2`.

The mesh/pointer browser trial and later production project gate are separate; results are recorded only after they terminate.

The first unmodified standalone-page pointer probe reached the real `turn crank` handle, observed DOM `pointerup` and terminal `completed`, and changed the visible crank readout from 0 to −1° while all other 23 input readouts were unchanged. The probe's `--partial-turn` acceptance asserts a *positive* crank delta, so the run correctly exited 1; its failure screenshot then also timed out after 60 seconds under concurrent Chromium work. The report is preserved at `_build/follow_viewer/trial-standalone-crank.json`. A second probe uses the opposite drag; its result is not predeclared.

The exact-point second probe did not find a visible hover hit and exited 1; its own report is preserved. A full hover-search third probe found the handle and observed pointerup, terminal `completed`, visible crank 0→+1°, and all 23 other inputs unchanged, but the harness again exited 1 solely because its Playwright screenshot timed out at 60 seconds after fonts loaded. Its report `_build/follow_viewer/trial-standalone-crank-positive-search.json` remains `validation: pending`, not green.

The viewer-owned `trial_browser_probe.py` then mounted this exact mesh export in hosted Chromium with the public viewer/run API and `dt=.1`. It passed all eight Python-oracle stages, comparing all 214 coordinates at rest, lift, blocked crank, replay, relief, after-relief 90°, outward 90° and return 360°. The blocked angle was bit-exact `.36119713971311285`, replay snapshot was identical, the outward ball remained retained on return, API 25 and all 25 controls were present, and there were no page errors. This process exited 0; report `_build/follow_viewer/trial-hosted-full-bank.json` and screenshot `trial-hosted-full-bank.png`. The screenshot required a 180-second cap rather than the old standalone probe's 60-second cap, and I visually inspected the coherent assembled model and readout at returned crank 360°. This is public hosted run coverage and visual evidence, not a pointer gate. Paused baseline-versus-trial capture diagnosis follows separately.

The `capture_latency_probe.py` paired a frozen 213-coordinate v11 export against the 214-coordinate v12 trial on **the same API-25 bundle and reserved CPU 14**, with each run paused at tick 0 and no command/pointer. Both have 149 parts and 40 MB of export assets. At 800×600, 20 rendered frames took 388.3 ms (v11) and 384.4 ms (v12), yet Chromium/SwiftShader screenshot readback took **46.855 s** and **64.011 s** respectively. Both captures exited 0, stayed at tick 0 and had no page errors. Reports/screenshots are `_build/follow_viewer/capture-v11-paused.*` and `capture-v12-paused.*`. The v12 sample crossing the old project harness's 60-second screenshot cap even while paused explains its timeout without an idle Follow evaluation or rendering-loop regression. It is a slow capture floor, not proof of a product runtime defect; the viewer's own public `capture.py` already uses a 300-second screenshot cap. The final standalone pointer run below kept every functional check and extended only that capture cap.

That final standalone run **exited 0**: an external wrapper changed only Playwright `Page.screenshot`'s timeout to 180 seconds before executing the unchanged project `operating_standalone_probe.py`. No viewer code, page script or machine state was patched. The real auto-mounted standalone page found `turn crank` by hover, observed DOM pointer release and terminal `completed`, changed the visible crank readout 0→+1°, kept all 23 other input readouts unchanged, and reported no page errors. `_build/follow_viewer/trial-standalone-capture180.json` has `validation: passed`; its PNG was visually inspected as a fully assembled Curta with 1.0000° and `completed` on the inspector. This is a one-degree real pointer gate at the default timestep, not a completed full-turn tempo claim.
