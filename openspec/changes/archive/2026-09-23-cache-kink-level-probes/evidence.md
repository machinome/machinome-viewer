# OperatingCurta kink-probe evidence

This is a standalone viewer performance cycle under the pilot's standing autonomous framework/viewer-fix direction, not a claim of design-specific pilot ratification. Project and framework sources are read-only to this cycle.

## Frozen source and measured need

- Viewer baseline: clean main `08d70fbaac976d05e896e5c33234d438c4f3085c` (runtime content `572648f`); cycle worktree `WTs/cache-kink-level-probes`.
- Actual production radial-ball export: `projects/Calculators/Curta-Type-I-3x/_build_checks/operating-radial-ball-a139fe0/manifest.json`, SHA-256 `bab5b1248bf8f100b764bf37bfd00065770df876bb83ec191b44549a9727569b`. Python eight-stage oracle: same `_build_checks/operating-radial-ball-oracle-a139fe0.json` (SHA-256 begins `0c6d2409`). Neither is modified.
- Pinned V8 five-tick profile (CPU14, `.1 s` per tick) had 5,778 samples; `kinkBreaks` accounted for 1,904 inclusive samples (33.0%). Read-only esbuild instrumentation observed 1,058 kink searches, 421,392 level calls and 155,267 repeated same-kink/same-fraction calls (36.8%). Follow boundary cuts contributed 768 searches/129,622 repeats, maximum 1,898 distinct keys per search; law motion 225 searches/24,600 repeats, maximum 154. The 2,048-entry per-call ceiling covers the observed largest search.
- The first in-memory prototype, which was deliberately not production-safe because it did not certify callbacks, measured paired CPU14 baseline `4.730731`, candidate `3.696975`, then baseline `4.617393`, candidate `3.730114` seconds. All five complete 214-coordinate bank bit hashes and 768 ordered Bound probes matched. This motivated the certified implementation, not its acceptance.

## Current isolated candidate

- Red-first new focused test initially failed because `certifiedKinkBreaks` was absent. The implementation was subsequently narrowed further: `expressionKinkBreaks` accepts no callback and builds the exact published `kinkLevel` evaluation itself; generic `kinkBreaks` remains eager. Recursive numeric-graph/binding proof excludes random, indirect/custom calls and coercing shapes. Preflight failure falls back; only finite successful values enter a search-local 2,048-entry cache. Negative-zero fractions and nonfinite fractions bypass it.
- Eight focused cache tests pass; 177 combined cache/jump/run tests pass. Independent reviewer initially found no source blocker and requested the callback-free refinement; final independent review is requested on the frozen diff before integration.
- Exact same harness, source/bundle unchanged outside the isolated worktree: `taskset -c 14 node openspec/changes/archive/2026-09-23-follow-prefix-replay-cache/curta_prefix_probe.mjs <frozen-manifest> free`. First certified pair: viewer main `4.708914` CPU seconds, isolated candidate `3.846994` (18.3% lower). A later pair under heavier shared-host load: main `5.119896`, candidate `4.630695` (9.6% lower). After the callback-free refinement and small-kink eager fallback, a quieter pair measured main `4.271973`, candidate `3.570818` (16.4% lower). These pairs had 768 identical ordered Bound levels (SHA-256 `6475ffee42fa40699960e20442859b09531be654bbead69adecc24dc52983943`), 394 prefix derivations and five exact 214-bank IEEE hashes:
  1. `90ea04adb3157d416143aa673835967f4b2eeb78c9598de1ff8393cf1207d569`
  2. `9be023d16e993161b9e079d1773cb98e2eb0a4e0892d4d830f93174e524c9d8b`
  3. `5570a2db044a543de5a0c70a1f57cfeb2d439c582069e998cb537196071a0aa0`
  4. `e68f731d71afc317f96e25b841b61feead3c134e7b0309169be4d8d3be0b3fe6`
  5. `08639ca6fa4fca701796575db5fb7ad131e57b69363ed33543a1ee7144b57af6`
- `taskset -c 14 node openspec/changes/cache-kink-level-probes/curta_stage_probe.mjs <frozen-manifest> <frozen-oracle>` exited zero: all eight Python-oracle stages had exact 214-coordinate bank equality and statuses, stopped crank `0.36119713971311285`, exact snapshot replay, completed return to 360°.

## Gates and caveats

- Isolated widget `npm run typecheck`, `npm run build`, and focused `npx vitest run src/run/kink-probe-cache.test.ts src/run/jumps.test.ts src/run/run.test.ts` pass (177/177). An initial full widget run had 1,501 pass and one absolute throughput test fail: synthetic six-self-read-dial fixture measured 882 ticks/s versus required >900 under shared load. Unchanged main's targeted test also failed on pinned CPU14 at 849 ticks/s; candidate was 818 in that window. The final implementation bypasses proof/memo for fewer than three kinks (actual Curta has 25–116); a later pair was main889, candidate877, both below the same threshold. Under a quieter host the unchanged threshold passed at 1,028 ticks/s, and the final complete `taskset -c 14 npm test -- --run` passed 56/56 files, 1,502/1,502 tests in 108.46 seconds. No tolerance or threshold changed.
- Initial isolated Python `pytest -q` before a build failed six tests because the worktree lacked `dist/machinome-viewer.js`; after the isolated build, bundle/CLI/server subset passed 39/39. A post-build full suite entered browser tests during unrelated SwiftShader contention and its own PID was stopped after ~6 minutes to avoid overlap. The final serial post-build `/home/asa/devel/machinome-studio/.venv/bin/pytest -q` passed 196 tests and 22 subtests in 462.10 seconds (51 deprecation warnings).
- Hosted Chromium validation used the final isolated bundle SHA-256 `feeaed32febb1ede3b267e9f47a6a0fd0a6b39bc149453af338011489b68ff86` and archived `curta_browser_probe.py` on the frozen project export/oracle. It passed all eight exact 214-bank stages, statuses, blocked stop/replay and 360° return, with no reported differences or page errors; report `/tmp/viewer-kink-browser.json` SHA-256 `d3abc75707c35bf5ff356fa046156ede8304d177ae793b83ac2b57f5755e5782`. The primary viewer bundle and root's copied bundle SHA `0d602f...` were not rebuilt or edited.
