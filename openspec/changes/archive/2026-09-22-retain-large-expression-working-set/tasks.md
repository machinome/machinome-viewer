## 1. Reproduce the cache churn

- [x] 1.1 Add a viewer-owned red-first test that prepares a >102,690-node fitting graph, then checks consecutive outer operations keep its generation and values; show it fails with the 50,000-node default.
- [x] 1.2 Record the pinned OperatingCurta and CounterTensOperatingTrial export identities, current cache generation counts, and the same-export 48-tick pre-change timing/bank/memory baseline.

## 2. Retain the measured working set

- [x] 2.1 Raise only the finite expression-node trigger to 125,000 and make the red test green; keep scope, generation reconstruction, low-limit pressure and final-mount disposal behavior unchanged.
- [x] 2.2 Run focused expression-lifetime and running tests, the relevant corpus, typecheck, and build checks; verify two current machines fit concurrently and last-mount disposal empties the graph.

## 3. Validate on the originating machines

- [x] 3.1 Re-run the exact pinned OperatingCurta 48-tick segment with the new default on the reserved CPU; compare all 213 bank values, admitted travel, generations, time and memory to the baseline.
- [x] 3.2 Exercise the current CounterTensOperatingTrial export and a proportionate Chromium current-export control/bank check with the rebuilt bundle; record the remaining full-turn latency honestly.

## 4. Integrate the record

- [x] 4.1 Update the viewer changelog and amend ADR-043 with the measured threshold/memory trade-off; sync the accepted delta to the baseline viewer-package spec.
- [x] 4.2 Validate the complete change artifacts, source diff and evidence with all implementation tasks checked before archival and viewer-main integration.
