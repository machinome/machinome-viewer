# Follow prefix replay evidence

## Origin and frozen identity

OperatingCurta's production radial-ball export is `projects/Calculators/Curta-Type-I-3x/_build_checks/operating-radial-ball-a139fe0/manifest.json`, SHA-256 `bab5b1248bf8f100b764bf37bfd00065770df876bb83ec191b44549a9727569b`. Its Python eight-stage oracle SHA-256 is `0c6d2409d50a8cd5f9e7071a32d20c7018bcbfee39d6034b7481bfc310b38962`. This is a real 214-coordinate, 25-control project export, not a synthetic-only benchmark. Source is unchanged by this viewer cycle. Viewer baseline is main `fe1a7082d1f4e7907ae50c0268e6d26fd19e444b`.

Read-only baseline probe on CPU 15 without WebGL found 20 searched Bounds and 768 `constraintLevel` calls in five `dt=.1` ticks of crank 0→90 over 0.5 seconds. The ball's low/high Bounds have the same ordered three-edge prefix (two source laws, one terminal Follow), and its retained output is absent from `trace.motions`; the other two Bounds use determined paths. Baseline derived 773 prefixes, 768 inside the Bound samples. This named same-fraction duplicate is the optimization target.

## Red-first and semantic gates

The new synthetic paired-Bound test failed before implementation: 133 derivations versus 132 sampled levels, where sharing requires fewer derivations than levels. After implementation it passes. Focused tests also cover two distinct Follow edge sequences, snapshot restore freshness, a constant-shaped `random(1)` source (which is deliberately not cached), and repeat of a positive one-sided unsupported-precision error with unchanged atomic snapshot. The cache excludes zero/nonfinite fractions, non-law ancestry, multiple/nonterminal Follow, stateful/indirect/overridden calls, and failed prefix propagation. Each Bound graph is evaluated separately on every original sample.

The diagnostic `curta_prefix_probe.mjs` hashes sorted bank names and IEEE-754 double bits at every tick, and hashes ordered `(Bound id, side, fraction bits, level bits)` observations. On the frozen export, two paired CPU-15 runs gave:

| Case | Baseline CPU | Candidate CPU | Bound samples | Baseline→candidate derivations | Identity |
| --- | ---: | ---: | ---: | ---: | --- |
| Free crank, first five 0.1 s ticks | 6.086 s | 4.455 s | 768 both | 773→394 | All five 214-bank bit hashes equal |
| Repeat free run | 5.651 s | 4.497 s | 768 both | 773→394 | Ordered sample digest `6475ffee42fa40699960e20442859b09531be654bbead69adecc24dc52983943` both |
| Raised collar, first blocked tick | 1.655 s | 1.627 s | 342 both | 346→243 | Ordered sample digest `11109fd698d437d3b70eef9ffc0b7a7edd3b8c09e55acc9ec9bc7d167065cfee` both; blocked at exactly 0.36119713971311285°, same 214-bank bit hash, restore/replay exact |

After moving structural eligibility inspection behind the first successful prefix, a final candidate-only repeat produced the same free/raised ordered trace digests, 214-bank bit hashes, outcomes and 394/243 derivation counts; free CPU was 4.600 s and raised CPU 1.694 s. The raised-tick CPU difference is too small to claim a reliable speedup; the duplicate-work reduction and exact trace are the supported result. Neither case makes a declared operation real-time. The first five free ticks still take about 4.5 CPU seconds for 0.5 seconds of simulated time without WebGL. No timestep, sample count or physical law was changed.

## Browser and package checks

On a rebuilt candidate bundle, the hosted public `MachinomeViewer.mount`/`run` path loaded the frozen production manifest in Chromium/WebGL, reported API 25 and 25 controls, and matched the frozen Python oracle at rest, lift, blocked contact, replay, relief and completed retry—every one of the 214 coordinates at each stage, with no page error. First candidate bundle SHA-256 `e7d565f3aca581ba743f77a67262ad9571e2fd31fc7e5d0c89225ed6238b8076`; its browser report and inspected screenshot are preserved in ignored worktree `_build/follow-prefix-browser.json` and `.png`. The screenshot shows a coherent assembled calculator and visible running controls, not a headless-only pass. After tightening eligibility to the actual producer law*→single Follow contract, bundle SHA-256 `6d1e0c9e052f2e2ea2d96f7f33ae665d056da9ef93337cd182bdda81f79528d8` passed all eight hosted 214-bank oracle stages—including outward push and retained return to 360°—with exact statuses and no page errors. Its report is `_build/follow-prefix-browser-final-eight.json`. The final preflight-order repair built bundle SHA-256 `0d602f50532ffe3c906b0bea285352cf60c5165da9b77d818d948f75f5e113df` and passed the same eight stages in `_build/follow-prefix-browser-final2-eight.json`. Both final runs omitted a redundant screenshot, not state checks.

`npm run typecheck` and 203 focused run/program/source-timing tests passed after the scope tightening and an injected-prefix-failure atomic retry test. Two broad `npm test` attempts before that last test each passed 1,492/1,493 tests; their only failure was the same unrelated no-Follow ShiftedCarry throughput floor at 2,991 and 2,980 ticks/s against 3,000 on the shared host. Isolated rerun of exactly that test on CPU 15 passed at 3,146 ticks/s. On final source, the full suite excluding the CPU floor passed 1,480/1,480 tests in 54 files. The isolated floor pass covers the remaining 14-test file's failing case, while the other 13 cost tests passed in both broad runs. Neither broad process is relabeled green.

An independent reviewer reran `run.test.ts` after the injected-failure test (85/85), checked the cache's one-`reachedBounds` lifetime, exact edge/fraction key and separate Bound graph evaluation, and found no demonstrated semantic blocker. The reviewer noted a hypothetical first-error ordering risk if structural eligibility inspection ran before a later prefix failure. The final source now performs that optional inspection only after the ordinary prefix succeeds, and disables reuse if inspection itself fails. The ordinary prefix's first `t=0` evaluation remains uncached; injected failure and retry, production ordered traces and eight-stage browser parity all pass after this repair.
