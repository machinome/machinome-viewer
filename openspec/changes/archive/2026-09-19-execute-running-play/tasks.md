## 1. Red contract tests

- [x] 1.1 Add red loader tests for the exact play edge, every malformed field, invalid initial interval, unsupported root/cycle/writer, and play fan-out.
- [x] 1.2 Add red engine tests for either-flank pickup, exact/non-integer reversal, split requests, three-stage cascade, snapshot/restore/reset, and atomic refusal.
- [x] 1.3 Add the red two-gap ranged-follower stop proving `x=40,y=30,z=20` and a release-side bound that leaves the input free.

## 2. Loader and engine

- [x] 2.1 Extend running document types and `loadProgram` with strict play-edge validation and driver-rooted linear-chain derivation.
- [x] 2.2 Execute play projection in program order through the ordinary bank without changing existing law, self-read, block, wiring, or formula paths.
- [x] 2.3 Replay play prefixes from the originating input during stop localization and atomically commit the same replayed prefix.
- [x] 2.4 Verify worker protocol, in-thread fallback, commands, snapshots, restore, reset, and republish need no second play state.

## 3. Version and parity

- [x] 3.1 Raise the single declarations to viewer API 22 and document versions 1 through 9; update JS and Python capability tests.
- [x] 3.2 Copy the framework's regenerated running corpus byte-for-byte once available and extend its coverage guard for every play feature.
- [x] 3.3 Replay the complete corpus and retain focused tests for hazards or malformed shapes the corpus does not exercise.

## 4. Browser and documentation

- [x] 4.1 Add and run a real-page smoke mounting a version-9 play document, issuing a request, and observing the posed retained coordinate.
- [x] 4.2 Update README and unreleased changelog with play execution, API 22, document version 9, and the narrow non-physics scope.
- [x] 4.3 After behavior is proven, add the viewer-owned ADR, update the ADR index, and record implementation evidence.

## 5. Validation

- [x] 5.1 Run widget typecheck, focused and full Vitest, Python tests, build/currency checks, and available browser integration; report skips and environment failures.
- [x] 5.2 Validate OpenSpec and leave implementation uncommitted for adversarial review, with no archive, integration, push, or publication.
