## 1. Pin the failing endpoint and paired contract

- [x] 1.1 Add red public running-document tests for `to=-4.9425` itself, then `to=3.9075` against the inclusive upper bound, and for the separately reached adjacent start `-4.942499999999999`; assert exact input/descendant bits, completed status and no false stop.
- [x] 1.2 Add red controls for a genuine outward target, a multi-tick final endpoint with bit-identical intermediate admissions, a partial stop before the endpoint, signed zero, `by`/rate unchanged, affine chains with retained offsets, jump/self-read relations, and active-command snapshot/restore/replay.
- [x] 1.3 Pin the framework producer's corresponding fixture/outcome and the originating Curta export identity; compare the viewer's ordered outcome, stop and full bank rather than a scalar-only readout.

## 2. Preserve the target through the running path

- [x] 2.1 Retain an absolute request's converted native target independently of its delta in `Command` and its active snapshot record, with legacy-record fallback and atomic malformed/wrong-type/nonfinite target refusal; keep the existing interior ramp arithmetic.
- [x] 2.2 On full terminal admission, thread the exact target through the input's endpoint and determined descendants before bound judgment; preserve intermediate samples and use the ordinary path for partial admissions, `by` and rates.
- [x] 2.3 Prove static and dynamic bounds, first errors, command reporting, committed bank, worker/in-thread operation and restore/replay use one consistent endpoint without a tolerance or status relabel.

## 3. Verification and completion

- [x] 3.1 Run focused running/command tests, full widget suite and typecheck; execute a hosted public-handle browser regression on the pinned producer fixture without competing with the root's current Curta browser job.
- [x] 3.2 Run the originating Curta case after the paired framework fix, retaining real outward-stop/relief and unrelated control behavior; document any environmental browser limitation honestly.
- [x] 3.3 Validate the OpenSpec change, sync its baseline spec, archive evidence after root review; do not push or publish. Local integration remains the following repository step.
