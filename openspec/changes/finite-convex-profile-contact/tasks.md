## 1. Freeze paired evidence and red tests

- [ ] 1.1 Pin the framework's version-13 document fixture and numeric profile-contact corpus, the Curta native-plus-mesh exploratory table SHA, baseline viewer/API/document versions and a no-feature version-12 control; record that project installed-print/axial proof remains separate.
- [ ] 1.2 Add red viewer tests for version/table validation: finite points, literal table indices, repeated vertices, zero/backtracking edges, nonadjacent self-crossing pentagram, exact-binary convexity with valid collinear and near-collinear examples, wrong arity, reserved-name collisions, missing table, legacy version, combined Follow plus profile version 13, and unsupported call reachability.
- [ ] 1.3 Add red paired pointwise tests for strict gap, endpoint touch, overlap, disjoint AABB, later polygon pair contact, exact degree/trig/transform/axis/projection order, signed-zero ties, reversed arguments, nonfinite placement, huge finite angles, transformed collapse/overflow even with disjoint other-profile AABBs, and actual Curta states; retain the producer's ordered expected results.
- [ ] 1.4 Add red runtime tests for direct and binding-aliased Bound calls, first-error/atomic refusal, two simultaneous mounts with identical expression and bank but different profile tables, snapshot identity/restore, and unchanged version-12 replay.
- [ ] 1.5 Pin a no-mesh document from the actual installed Curta contact trial and independently count viewer predicate calls, distinct exact pair keys, placement calls and distinct placement keys on one integration attempt. Preserve its document identity, ordered contact/bound samples and full bank as the before/after oracle; reject a cache if viewer repetition or material CPU benefit is absent.
- [ ] 1.6 Add red counted-work tests for same-tick pair and placement repetition, finite IEEE keys including +0 versus -0, a failing right placement after valid left, nonfinite/custom/coercing operands, eager expression operand errors, bounded eviction, cross-tick/restore and simultaneous-mount isolation; no test-only host API.

## 2. Load and evaluate the finite data

- [ ] 2.1 Implement version-13 profile table and call validation in the running loader, preserving producer polygon/index order and rejecting unsupported data before mount; advertise viewer API 26/document version 13 only with the accepted capability.
- [ ] 2.2 Implement one document-owned immutable table context and the ordered rigid XY placement, world-AABB and inclusive-contact SAT predicate; validate all transformed edges before pair AABB early return, keep polygon data out of expression nodes and bound retained work to the program lifetime.
- [ ] 2.3 Thread profile context through generic expression evaluation and pass equivalence; make determined-bound PathValue execute that same context or preflight-fallback before partial work, including calls reached through bindings.
- [ ] 2.4 Keep numeric Bound expressions, fixed samples/bisection, admission, committed check and snapshot APIs unchanged; verify the new call only supplies a scalar 0/1 used by authored limits.
- [ ] 2.5 After the empirical viewer gate, add private per-Run-integration-attempt successful-value reuse with pair LRU cap 1,024 and placed-profile LRU cap 256. Use immutable loaded-profile identity and finite primitive-number IEEE keys; bypass both caches for the whole call if either placement is uncertain/custom/nonfinite. Publish new placements only after both placements and pair evaluation succeed; never cache failures. Expire the cache in `finally` for success or refusal, with no cross-attempt/mount retention or public resource ceiling.

## 3. Paired verification and completion

- [ ] 3.1 Compare viewer and framework v13 corpus results and full numeric Bound outcomes, including touch/gap, large-angle/overflow cases and actual Curta-sized data; resolve any near-tangent parity discrepancy without a blanket epsilon or changed contact rule.
- [ ] 3.2 Benchmark table size, expression-node count, memory and CPU against the capped scalar formula on the same pinned profile data; verify finite AABB/SAT early exit and no unbounded cross-document retention.
- [ ] 3.2a Compare actual installed-trial viewer before/after counted work and CPU on the same pinned document, bank and ordered samples. Record pair/placement hit rates, capped retained entries and all failures honestly; verify legacy, multi-mount and error paths stay unchanged.
- [ ] 3.3 Run focused and full widget tests, typecheck, Python tests, isolated bundle/browser hosted pointer and snapshot/restore checks, legacy-version checks, and strict OpenSpec validation; require a bounded current Curta local-Bound trial and source-profile witness while separately reporting unfinished installed-print/axial/full-motion production gates.
- [ ] 3.4 Red-first verify the source-only API 26/document-13 statement, numeric-Bound/pointwise scope and recorded 0.7.0/API-25 baseline preservation in the viewer documentation structure test. Separate the home note's baseline API 25/documents 1–12 facts from the source-derived API/document substitutions so the source bump cannot retroactively alter that record; test each against its owning record and make no unsupported push/upload claim. Update only the owning compatibility and running reference, the Unreleased API 26 changelog section and source-status wording as needed. Keep the CLI `describe` example source-derived. Build strict Sphinx HTML, inspect changed built pages, run `scripts/check-docs-browser.py`, and run `scripts/check-dist` if README, `pyproject.toml` or `MANIFEST.in` changes. Do not add a mount API, project/workspace provenance to reader pages, or a new teaching page.
- [ ] 3.5 After root review and implementation authority, synchronize specs, consider a viewer-owned ADR, archive evidence and integrate only the clean verified viewer cycle against the then-current main without pushing or publishing.
