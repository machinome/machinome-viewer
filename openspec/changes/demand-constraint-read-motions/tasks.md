## 1. Red proof and frozen baseline

- [ ] 1.1 Record the frozen export identity, compiled crank bound reads, demanded set and baseline first-tick replay/path counts, ordered bound samples and full bank.
- [ ] 1.2 Add a viewer-owned red-first self-read bound test that requires an exact retained path without changing the stop, bank, replay or error behavior.

## 2. Demand only actual bound reads

- [ ] 2.1 Extend tick-local motion demand with constructible compiled constraint reads; leave tracing, fallback, law and stop search untouched.
- [ ] 2.2 Make the red test green and prove exact ordered actual Curta bound samples, stop result and 213-coordinate bank against the same frozen export.

## 3. Cost and regression gate

- [ ] 3.1 Compare paired no-WebGL actual-export first-tick CPU, replay counts and path construction without assuming Python speedup; reject any material parity drift.
- [ ] 3.2 Run focused run/corpus/source-timing tests, typecheck and build, then the broad suite; report wall-clock gates and browser scope honestly.

## 4. Durable integration

- [ ] 4.1 Record the finding and measured result in viewer evidence and changelog, sync the accepted viewer-package spec, and archive the OpenSpec change.
- [ ] 4.2 Commit in the isolated viewer worktree, reconcile current main without discarding intervening work, fast-forward it after validation and rebuild/smoke the installed bundle. Do not push.
