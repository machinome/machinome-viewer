## Why

Curta's actual exported lockout bench stops a 120 -> 150 degree request but
refuses 120 -> 840 with a run invariant error. Framework main e63700e now
attributes pushing inputs at the first contact rather than a later free
endpoint; the independent viewer must execute that same corrected contract.

## What Changes

- Preserve the located contact bracket and use it to identify pushing inputs.
- Keep numeric/static-read stops, atomic refusal, independent admissions and
  the existing sampling limits unchanged.
- Consume the producer's exact running corpus and require periodic-contact
  coverage; validate the actual source-backed Curta browser reproduction.
- Record the correction without a public API or document-version change.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

- `viewer-package`: a periodic moving-read bound stops at first contact even
  when the requested endpoint is free again, with producer corpus parity.

## Impact

Viewer run executor, tests, committed producer fixture, specs and decision
record. No framework imports, geometry changes, UI changes, package release
or remote publication. Curta's operating adoption remains project work after
paired acceptance. The pilot authorized the matching correction and continued
work, including routine validation/integration, on 2026-09-20.
