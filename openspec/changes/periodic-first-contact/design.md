## Context

Viewer base 4355da1; producer e63700e; Curta reproduction 21c7502. The pilot
approved implementing the matching viewer correction and continuing without
routine approval stops. This cycle consumes producer ADR-135, amending the
endpoint attribution in viewer ADR-054 and its baseline step requirement.

## Goals / Non-Goals

Goals: first-contact attribution, unchanged producer corpus parity, real Curta
browser proof, integration and continuation. Non-goals: contact dynamics,
geometry fitting, new APIs, better sampling, compound-only push support,
clocked changes, or a claim that all Curta restraints are complete.

## Decisions

1. Carry an ephemeral inside/outside contact pair from search through event
   selection. Commit at inside, evaluate each candidate alone at both fractions
   from the same original stretch, and stop it only if its level increases.
   Endpoint comparison is contradicted by the actual periodic obstruction.
2. Preserve numeric/static-read paths, frozen own values, no-snap assertions,
   simultaneous contacts, time admissions and transactional refusals. Stopping
   every candidate would incorrectly stop relieving or disengaged inputs.
3. Copy the producer corpus byte for byte and mirror its periodic coverage
   guard. The fixture, not an independent arithmetic answer, pins agreement.
4. Validate source tests and a newly built bundle through Curta's existing
   isolated browser probe. Broader five-flat diagnostics and full-root adoption
   remain project work. No use of the pilot's live browser session.

## Risks / Trade-offs

- Existing finite samples can miss a narrow unsampled obstruction; unchanged.
- If no candidate alone pushes, the tick still refuses atomically; unchanged.
- Consumer-only arithmetic drift: full corpus plus targeted upper/lower,
  time, simultaneous, relieving/disengaged and replay tests.
- Restore clears record rings: preserve the producer's separate quiet restore
  step; its unrelated harness cursor finding is not part of this correction.

## Migration Plan

Red tests, minimal executor edit, full viewer validation, build, actual Curta
acceptance, sync/archive and commit. Fast-forward clean unchanged viewer main
under the pilot's continuation authority, preserving the branch. No release,
push or package-version change. If main advances, inspect before integration.
