## 1. Evidence and red tests

- [ ] 1.1 Pin the production Curta export, baseline CPU and all five full-bank/ordered-Bound hashes; identify contributing expression-backed call sites and a finite cache ceiling.
- [ ] 1.2 Add red tests for certified repeated probes and exact cuts, plus generic/stateful callback, random/custom binding, different kink, signed zero/nonfinite, first error/retry and bounded retention negatives.

## 2. Narrow implementation

- [ ] 2.1 Implement recursive deterministic graph/binding eligibility for expression-backed kink operands, with conservative fallback.
- [ ] 2.2 Implement successful-value, search-local bounded probe reuse behind an internal opt-in; leave generic `kinkBreaks` eager.
- [ ] 2.3 Opt in the proven running-law and Follow expression call paths only; verify independent review findings.

## 3. Verification and completion

- [ ] 3.1 Run focused viewer tests, full widget and Python suites, typecheck, strict OpenSpec validation and build in the isolated worktree.
- [ ] 3.2 Compare paired actual Curta CPU, every prescribed ordered Bound sample and every 214-bank bit hash across five ticks, plus eight-stage stop/replay/relief and hosted browser behavior on an isolated bundle.
- [ ] 3.3 Sync the observable spec, archive the change, record evidence, commit the completed cycle, reconcile concurrent viewer main without losing history, and integrate locally only after revalidation.
