## Why

The exported Operating Curta fails an ordinary input-9 crank turn when its
experimental tens restraint observes the carry lever. The browser reproduces
the producer's mixed moving-threshold landing error and retains its pre-request
state. A producer-only correction would leave makers unable to perform the turn.

## What Changes

- Correct the running executor's local landing orientation when a moving
  threshold overtakes its driven coordinate, preserving nearest-float landings.
- Match the ratified producer precision correction: algebraically proved
  constant relative contact must not be mistaken for branch departure by
  floating-point noise. Do not add an epsilon or suppress genuine refusals.
- Preserve genuine invariant refusals, transactional state, stationary-threshold
  behavior, clocked execution, API 23 and document versions 1–10.
- Pin parity with producer-owned conformance exports and inspect the real
  Curta browser request using the newly built bundle.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

- `viewer-package`: valid running motion remains valid when a moving engagement
  threshold overtakes the part, including observation by a restraint.

## Impact

Running jump executor and tests, copied conformance fixtures, and viewer-owned
acceptance records. Framework and Curta remain independent repositories. No
print changes, new public interface, release, push or publication is included.
The pilot explicitly approved this matching correction and local integration
after its explanation on 2026-09-21.
