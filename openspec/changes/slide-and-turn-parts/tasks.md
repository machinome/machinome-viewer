## 1. Reader and pure gesture planning

- [ ] 1.1 Add red reader tests using the paired producer's prismatic and composed-joint documents, including malformed spans, mismatched domains, occlusion-independent validation and duplicate same-kind controls.
- [ ] 1.2 Accept Slide and selected placement spans, retaining legacy no-span validation and no-control behavior; prove current frame math for offset pivots, non-parallel joints and moving ancestors.
- [ ] 1.3 Add red signed sliding-gesture tests for both ratio signs, view degeneracy, pointer reversal, a partially admitted stop and absence of delayed requests; implement through the existing request planner.

## 2. Direct pointer interaction

- [ ] 2.1 Add red browser tests for selector sliding, crank clicking and separately selectable axial/turning handles using real pointer input and public controls() positions.
- [ ] 2.2 Implement hover/selection handles and touch reachability, preserving instruction clicks, camera ownership, visibility, focus, occlusion and all cancellation paths; make the browser tests green.
- [ ] 2.3 Extend public types/readback with Slide, selected placement metadata and reachable drag points; declare API 13 consistently through the bundle, handle and package description without changing document versions.

## 3. Paired proof and completion

- [ ] 3.1 Run pure tests, worker conformance and built-bundle browser regression; record the exact framework/viewer content pair and verify a composed fixture while its ancestors move.
- [ ] 3.2 Exercise and inspect the migrated Curta in standalone and normal hosted viewers; prove mechanical stops and retained results come from the run and record any independent project gaps.
- [ ] 3.3 Update README, changelog and the confirmed ADR-053 successor/index; sync and archive this repository's OpenSpec change and commit the completed evidence without pushing or publishing.
