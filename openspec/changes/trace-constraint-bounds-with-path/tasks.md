## 1. Establish exact baseline and red proof

- [ ] 1.1 Record pinned post-cap Curta export/source hashes, active-stop sample trace and full 213-bank result on the current generic search; separate profiler overhead from unprofiled wall time.
- [ ] 1.2 Add a viewer-owned red-first searched-bound test whose standing-heavy expression exceeds a countable resolution ceiling under the generic path, while asserting exact bank, stop, outcome and snapshot/replay behavior.
- [ ] 1.3 Add or identify exact tests for signed-zero traced endpoints, unsupported-path fallback, native error propagation and nontraced search.

## 2. Search-local path implementation

- [ ] 2.1 On the determined traced branch only, bind one reconstructible bound path at the first existing sample and use its moving cone at subsequent samples; choose moving names conservatively from path motion and IEEE-754 endpoint bits.
- [ ] 2.2 Retain the same scope, `Number` conversion, level subtraction, sampling schedule and fallback to generic evaluation for unsupported nodes; turn the red test green.

## 3. Project and regression acceptance

- [ ] 3.1 Compare ordered actual Curta active-stop sample argument/result bits, all 213 bank values, event records and replay to baseline on the same pinned export.
- [ ] 3.2 Measure same-export 48-tick and active-stop before/after cost on a reserved CPU, then exercise proportionate actual pointer browser control without claiming a full turn unless it completes.
- [ ] 3.3 Run focused running/expressions/corpus tests, full suite, typecheck and build; report unrelated wall-clock throughput failures honestly.

## 4. Durable integration

- [ ] 4.1 Amend the viewer changelog and running-path ADR, sync the accepted spec delta, and archive the OpenSpec change with measured evidence and all implementation tasks complete.
- [ ] 4.2 Validate diff and commit in this isolated worktree, fast-forward viewer main if clean, and rebuild/smoke the installed primary bundle without pushing.
