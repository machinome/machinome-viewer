=====================
Public API reference
=====================

The browser entry point is ``MachinomeViewer`` from ``machinome-viewer.js``.
Handles below are returned objects, not constructors you instantiate.
Method names are grouped by interface so they are searchable in the
:ref:`general index <genindex>`.

.. toctree::
   :maxdepth: 1

   mount
   viewer-handle
   layouts
   running
   clocked
   cli

Start with :doc:`../embedding` for a complete working page. The reference
documents the current public mounts and handles, not internal TypeScript
exports used as implementation/test seams.
