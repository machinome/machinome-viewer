__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
.pytest_cache/
.coverage
.env

# The widget's installed dependencies and built output. Anchored (not
# `node_modules/`) so a symlink at these paths is ignored too.
/solid_node_viewer/widget/node_modules
/solid_node_viewer/widget/dist

# The screenshots `tests/test_running_document.py` writes for inspection.
# Pixels are evidence, and the evidence is regenerated by running the
# test rather than committed as bytes.
/tests/_shots
